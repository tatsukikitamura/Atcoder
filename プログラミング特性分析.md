# プログラミング特性分析レポート

## 概要
競技プログラミング（AtCoder、paiza）およびヒューリスティックコンテストのコードを包括的に分析し、コーディングスタイル、アルゴリズム理解度、改善点をまとめました。

**分析対象**: 
- **115個のPythonファイル**（前回分析: 60個）
- **26個のC++ファイル**（ヒューリスティックコンテスト）
- AtCoder: ABC407, ABC408, ABC409, ABC412, ABC417, ABC421, ABC425, ABC429, ABC430, ABC431, ABC432, ABC434, ABC435
- paiza: A級、B級、C級、D級、S級の問題
- 就職活動: GENIEE, LINE, TEAMLABコーディングテスト, MoneyForward, donuts, m3, SanSan, Accenture, visional
- 大学課題、勉強用ファイル
- ヒューリスティックコンテスト: AHC001, AHC048, AHC050, AHC051, AHC056, AHC057

---

## 1. コーディングスタイル

### 1.1 命名規則

**特徴:**
- **大文字変数の使用**: 一部のファイルで`LIST`, `USE`, `COUNT`などの大文字変数が使用されているが、全体的には減少傾向
- **小文字変数の主流化**: 新しいファイル（ABC431以降、ABC435など）では小文字+アンダースコアの命名が主流
- **一貫性の向上**: テンプレートファイルや高品質なファイルでは適切な命名規則が使用されている
- **組み込み関数の上書き**: 一部の古いファイルで`list`を変数名として使用（改善の余地あり）

**具体例:**
```python
# ABC431/b.py - 大文字変数が残存
LIST = list(map(int,input().split()))
use = []
use2 = []

# ABC435/a.py - シンプルで適切
N = int(input())
count = 0

# ABC435/d.py - 適切な命名とクラス設計
class Node:
    def __init__(self,index):
        self.index = index
        self.nears = []
        self.arrival = False

# MoneyForward/5.py - 型ヒント付きの高品質なコード
def parse_datetime(date_str: str, time_str: str) -> datetime.datetime:
    return datetime.datetime.strptime(f"{date_str} {time_str}", "%Y/%m/%d %H:%M:%S")
```

**改善傾向:**
- 新しいコンテスト（ABC431以降）では命名規則が改善されている
- 就職活動のコーディングテスト（MoneyForward/5.py）では型ヒントと適切な命名が使用されている
- テンプレートファイル（BFS.py, DFS.py, SegTree.py）は高品質な命名規則を採用

**問題点:**
- 古いファイル（ABC407-ABC430の一部）では大文字変数が残存
- 一部のファイルで変数名が短すぎる（`x`, `y`, `i`, `j`など）
- 組み込み関数の上書きが一部のファイルで見られる

---

### 1.2 コード構造

**特徴:**
- **インデント**: 基本的に正しく使用されている
- **空行**: 関数間や論理的なブロック間に適切に使用されているファイルが増加
- **コメント**: 品質にばらつきがあるが、高品質なファイルでは詳細なコメントが存在
- **関数化**: テンプレートファイルや高品質なファイルでは適切に関数化されている

**具体例:**
```python
# Template/BFS.py - 詳細なdocstring
def bfs_with_node_class(start_node):
    """
    Nodeクラスのインスタンスを使用してBFSを実行します。
    ノードの 'arrival' 属性を訪問済みフラグとして利用します。
    
    Args:
        start_node (Node): 探索を開始するNodeインスタンス。

    Returns:
        list: 探索で訪問したノードのインデックスの順序リスト。
    """

# ABC435/d.py - クラス設計とコメント
class Node:
    """
    ノード情報の管理
    Attributes:
        index (int): ノード番号
        nears (list): 隣接リスト（隣接するノード番号を格納）
        arrival (bool): 探索済フラグ（到達済の場合Trueが返される）
    """

# ABC435/c.py - コメントが一部存在
domino = [0] * N
# 差分配列を使用した実装
```

**評価:**
- テンプレートファイルは非常に高品質（docstring、型ヒント、適切な構造）
- 新しいファイルではコメントが増加傾向
- 一部のファイルではコメントが不足している

---

## 2. アルゴリズム理解度

### 2.1 基本的なアルゴリズム

**理解度: 良好**

- **ループ処理**: ネストしたループを適切に使用
- **条件分岐**: if-elif-elseを適切に使用
- **配列操作**: リストの操作は基本的に理解している
- **ソート**: `sorted()`や`.sort()`を適切に使用

**具体例:**
```python
# ABC431/a.py - シンプルで明確
A,B = map(int,input().split())
if B >= A:
  print(0)
else:
  print(A-B)

# ABC432/a.py - ソートの使用
S = list(map(int,input().split()))
S.sort()
for x in range(3):
    print(S[2-x],end="")
```

---

### 2.2 中級アルゴリズム

**理解度: 中程度から高い**

- **DFS（深さ優先探索）**: 複数のファイルで実装、テンプレートも存在
- **BFS（幅優先探索）**: テンプレートファイルが存在し、適切に実装
- **動的プログラミング**: 基本的な理解はあるが、最適化の余地あり
- **差分配列**: ABC435/c.pyで実装されている

**具体例:**
```python
# ABC435/c.py - 差分配列の実装
domino = [0] * N
for x in range(N):
    if a_list[x] == 1:
        continue
    domino[x] += 1
    if x+a_list[x] < N:
        domino[x+a_list[x]-1] -= 1
    else:
        domino[N-1] -= 1

# A021.py - DFSの実装（改善版）
def dfs_paint(x, y, temp_grid, H, W ,use):
    if not (0 <= x < H and 0 <= y < W) or temp_grid[x][y] == 0:
        return   
    use.append([x,y])
    temp_grid[x][y] = 0
    for i in range(4):
        nx = x + DX[i]
        ny = y + DY[i]
        dfs_paint(nx, ny, temp_grid, H, W,use)
```

**評価:**
- アルゴリズム自体は正しく実装されている
- テンプレートファイルの存在により、再利用性が向上
- 一部のファイルでは関数の説明や引数の意味が不明確

---

### 2.3 高度なデータ構造

**理解度: 非常に高い**

- **BIT（Binary Indexed Tree）**: クラスとして実装（ABC430/c.py）
- **セグメント木**: 圧縮BITを使用したセグメント木を実装（ABC430/c.py）
- **セグメント木テンプレート**: Template/SegTree.pyに汎用的な実装が存在
- **差分配列**: 効率的な区間更新を実装（ABC435/c.py, ABC409/C.py）
- **グラフ構造**: Nodeクラスを使用したグラフ表現（ABC435/d.py, Template/BFS.py, Template/DFS.py）

**具体例:**
```python
# ABC430/c.py - 高度なデータ構造の実装
class BIT:
    def __init__(self, n):
        self.n = n
        self.data = [0] * (n + 1)

    def add(self, i, x):
        while i <= self.n:
            self.data[i] += x
            i += i & (-i)

class SegTreeCompressedBITs:
    def __init__(self, N, Pa, Pb):
        # 圧縮BITを使用したセグメント木の実装
        # ...

# Template/SegTree.py - 汎用的なセグメント木
class SegTree:
    """
    init(init_val, ide_ele): 配列init_valで初期化 O(N)
    update(k, x): k番目の値をxに更新 O(logN)
    query(l, r): 区間[l, r)をsegfuncしたものを返す O(logN)
    """
```

**評価:**
- 複雑なデータ構造を理解し、実装できている
- テンプレートファイルの存在により、再利用性が大幅に向上
- これは非常に高いレベルの理解を示している

---

### 2.4 アルゴリズムの多様性

**理解度: 高い**

実装されているアルゴリズム:
- **DFS（深さ優先探索）**: A021.py, ABC435/d.py, Template/DFS.py
- **BFS（幅優先探索）**: Template/BFS.py
- **動的プログラミング**: A052.py, ABC409/C.py
- **尺取り法**: A063_AI.py（最適化版）
- **累積和**: 複数のファイルで使用
- **二分探索**: `bisect`モジュールを使用（ABC430/c.py）
- **BIT・セグメント木**: ABC430/c.py, Template/SegTree.py
- **差分配列**: ABC409/C.py, ABC435/c.py
- **グラフ探索**: ABC435/d.py（DFSを使用したグラフ探索）

**評価:**
- 幅広いアルゴリズムを理解している
- テンプレートファイルの存在により、実装の質が向上
- 実装の質にばらつきがある（最適化版と非最適化版が混在）

---

### 2.5 ヒューリスティックコンテスト

**理解度: 高い（C++実装）**

- **C++での実装**: AHC051, AHC056, AHC057でC++を使用
- **高度なアルゴリズム**: ビームサーチ、ILS（Iterated Local Search）、ダイクストラ法など
- **時間管理**: `chrono`を使用した時間管理
- **データ構造**: `set`, `map`, `priority_queue`, `deque`などを適切に使用

**具体例:**
```cpp
// AHC056/approach/first/4.cpp
struct BeamState {
    vector<vector<Pos>> paths_list;
    set<Pos> path_cells;
    int total_steps;
    int total_C;
    bool operator<(const BeamState& other) const {
        if (total_C != other.total_C) return total_C < other.total_C;
        return total_steps < other.total_steps;
    }
};
```

**評価:**
- C++での高度な実装が可能
- ヒューリスティックコンテスト特有の最適化手法を理解している
- 時間制約を考慮した実装ができている

---

## 3. コード品質の問題点

### 3.0 ファイル構成の問題

**発見された問題:**

1. **一時ファイルの残存**
   - `tempCodeRunnerFile.py` が1個存在（ABC409/）
   - デバッグ用のコードが一部残っている

2. **未完成ファイル**
   - `ABC430/c_later.py`: 未完成の実装
   - 一部のファイルで不完全な実装が残っている可能性

3. **重複ファイル**
   - `ABC430/c.py` と `ABC430/c_later.py` が存在
   - `ABC408/3.py` と `ABC408/3AI.py` が存在（最適化版と非最適化版）

**評価:**
- ファイル管理は改善傾向（一時ファイルが1個に減少）
- 完成版と未完成版を明確に区別する必要がある

---

### 3.1 バグの傾向

**発見された問題:**

1. **未初期化変数の使用**
   - 一部のファイルで未初期化変数が使用される可能性
   - 変数使用前に初期化チェックが必要

2. **例外処理の不備**
   ```python
   # ABC431/c.py - try-exceptの使用
   def check_H_N(H_N,B_M):
       try:
           y = 0
           count = 0
           for x in range(max(N,M)):
               # ...
       except:
           return count
   ```
   - `except:`の使用は推奨されない（具体的な例外を指定すべき）

3. **インデックスエラーのリスク**
   - 一部のファイルで境界チェックが不十分な可能性
   - エッジケースの考慮が不足している場合がある

4. **効率性の問題**
   ```python
   # ABC435/b.py - O(N^3)の実装
   for x in range(N):
       for y in range(x+1,N):
           use = 0
           for z in range(x,y+1):
               use += a_list[z]
   ```
   - 最適化の余地がある実装が存在

**評価:**
- 基本的なバグは減少傾向
- 例外処理の改善が必要
- 効率性の意識が向上しているが、さらなる改善の余地あり

---

### 3.2 コードの冗長性と効率性

**問題点:**
- 同じロジックの繰り返し（一部のファイル）
- 効率的でない実装（ABC435/b.pyなど）
- 最適化版と非最適化版が混在

**改善傾向:**
- テンプレートファイルの存在により、重複が減少
- 新しいファイルでは効率性を考慮した実装が増加

**具体例:**
```python
# ABC435/b.py - 非効率な実装（改善の余地あり）
for x in range(N):
    for y in range(x+1,N):
        use = 0
        use2 = False
        for z in range(x,y+1):
            use += a_list[z]
        
        for z in range(x,y+1):
            if use % a_list[z] == 0:
                use2 = True
                break

# ABC435/c.py - 効率的な差分配列の使用
domino = [0] * N
for x in range(N):
    if a_list[x] == 1:
        continue
    domino[x] += 1
    if x+a_list[x] < N:
        domino[x+a_list[x]-1] -= 1
```

**評価:**
- 最適化版を作成する能力がある
- テンプレートファイルの活用により、効率的な実装が増加
- 最初から最適化を考える習慣が向上している

---

### 3.3 型安全性

**問題点:**
- 型ヒントが一部のファイルでのみ使用されている
- 変数の型が不明確な場合がある

**改善傾向:**
- 就職活動のコーディングテスト（MoneyForward/5.py）では型ヒントが使用されている
- テンプレートファイルでは型ヒントが使用されていない（改善の余地あり）

**具体例:**
```python
# MoneyForward/5.py - 型ヒント付き
def parse_datetime(date_str: str, time_str: str) -> datetime.datetime:
    return datetime.datetime.strptime(f"{date_str} {time_str}", "%Y/%m/%d %H:%M:%S")

def first_enter(id: str) -> None:
    if id in employee_ids:
        print("can")
    else:
        print("cannot")

# Template/BFS.py - 型ヒントなし（改善の余地あり）
def bfs_with_node_class(start_node):
    # ...
```

**評価:**
- 型ヒントの使用は散発的だが、高品質なファイルでは使用されている
- 一貫して使用することで、コードの可読性と保守性が向上する

---

## 4. 強みと弱み

### 4.1 強み

1. **高度なアルゴリズムの理解**
   - BIT、セグメント木などの高度なデータ構造を実装できる
   - 差分配列、尺取り法などの効率的なアルゴリズムを理解している
   - ヒューリスティックコンテストでC++を使用した高度な実装が可能
   - これは競技プログラミングにおいて非常に重要なスキル

2. **問題解決能力**
   - 複雑な問題にも挑戦している
   - 基本的なアルゴリズムは適切に使用できる
   - 最適化版を作成する能力がある

3. **実装力**
   - コードは基本的に動作する
   - アルゴリズムをコードに落とし込む能力がある
   - 複数のアプローチを試行している（最適化版と非最適化版）

4. **学習意欲と組織化**
   - テンプレートファイル（BFS.py, DFS.py, SegTree.py, Node.py）を作成している
   - 便利な関数をまとめている
   - コードの再利用性を意識している

5. **幅広い問題への対応**
   - AtCoder、paiza、就職活動のコーディングテストなど、様々な問題に挑戦
   - ヒューリスティックコンテストにも対応
   - 大学課題も含めて、幅広い範囲をカバー

6. **コード品質の向上傾向**
   - 新しいファイルでは命名規則が改善されている
   - テンプレートファイルの存在により、コードの質が向上
   - 型ヒントの使用が増加傾向

---

### 4.2 弱み

1. **コードの可読性**
   - 命名規則が一部のファイルで一貫していない（改善傾向）
   - コメントが不足している（一部のファイル）
   - コードの意図が読み取りにくい場合がある

2. **保守性**
   - コードの重複が一部のファイルで見られる（改善傾向）
   - 関数化が不十分な場合がある
   - 一時ファイルや未完成ファイルが残っている

3. **デバッグ能力**
   - 例外処理が不十分な場合がある（`except:`の使用）
   - エッジケースの考慮が不足している場合がある

4. **コード管理**
   - 完成版と未完成版が混在
   - 一時ファイルが残っている（改善傾向）
   - ファイル名の一貫性がない（`a.py`, `1.py`, `A.py`など）

5. **効率性の意識**
   - 最初から最適化を考える習慣が向上しているが、さらなる改善の余地あり
   - 一部のファイルで非効率な実装が存在

6. **型安全性**
   - 型ヒントの使用が散発的
   - テンプレートファイルにも型ヒントがない（改善の余地あり）

---

## 5. 改善提案

### 5.1 即座に改善すべき点

1. **ファイル整理**
   - `tempCodeRunnerFile.py` を削除
   - 未完成ファイルを整理（完成版と未完成版を明確に区別）
   - ファイル名を統一（問題名や番号で統一）

2. **例外処理の改善**
   - `except:`の使用を避け、具体的な例外を指定
   - エラーハンドリングを適切に実装

3. **変数の初期化**
   - 使用前に必ず初期化
   - 未使用変数を削除

---

### 5.2 短期（1週間〜1ヶ月）

1. **命名規則の統一**
   - 大文字変数を小文字+アンダースコアに変更（古いファイル）
   - 組み込み関数（`list`, `len`）を変数名として使用しない
   - 意味のある変数名を使用

2. **コメントの追加**
   - すべての関数にdocstringを追加
   - 複雑なロジックにコメントを追加
   - デバッグ用コメントを削除

3. **テンプレートファイルの改善**
   - 型ヒントの追加
   - より詳細なdocstringの追加

---

### 5.3 中期（1ヶ月〜3ヶ月）

1. **関数化とリファクタリング**
   - 重複コードを関数に抽出
   - 再利用可能な関数を作成
   - コードの構造化

2. **型ヒントの使用**
   - すべての関数に型ヒントを追加
   - テンプレートファイルにも型ヒントを追加
   - 型安全性を向上

3. **コードレビューの習慣化**
   - 提出前にコードを見直す習慣
   - バグの早期発見
   - エッジケースの考慮

---

### 5.4 長期（3ヶ月以上）

1. **最適化の習慣化**
   - 最初から効率性を考慮する
   - メモリ効率と時間効率の両方を考慮
   - アルゴリズムの選択を最適化

2. **テストの習慣化**
   - エッジケースの考慮
   - テストケースの作成
   - デバッグの効率化

3. **コード品質の向上**
   - ベストプラクティスの適用
   - 設計パターンの学習
   - コードの可読性と保守性の向上

---

## 6. 具体的な改善例

### 改善前（ABC435/b.py）
```python
for x in range(N):
    for y in range(x+1,N):
        use = 0
        use2 = False
        for z in range(x,y+1):
            use += a_list[z]
        
        for z in range(x,y+1):
            if use % a_list[z] == 0:
                use2 = True
                break

        if not use2:
            count += 1
```

### 改善後
```python
def is_valid_subarray(arr: list[int], start: int, end: int) -> bool:
    """部分配列が条件を満たすかチェック"""
    subarray_sum = sum(arr[start:end+1])
    for val in arr[start:end+1]:
        if subarray_sum % val == 0:
            return False
    return True

count = 0
for start in range(N):
    for end in range(start + 1, N):
        if is_valid_subarray(a_list, start, end):
            count += 1
```

---

## 7. 総合評価

### スキルレベル
- **アルゴリズム理解**: ⭐⭐⭐⭐⭐ (5/5) - 高度なデータ構造を実装できる
- **実装力**: ⭐⭐⭐⭐ (4/5) - テンプレートファイルの存在により向上
- **コード品質**: ⭐⭐⭐ (3/5) - 改善傾向にあるが、さらなる向上の余地あり
- **可読性**: ⭐⭐⭐ (3/5) - 新しいファイルでは改善傾向

### 総評
高度なアルゴリズムを理解し、実装できる能力は非常に高い。テンプレートファイルの作成により、コードの再利用性と品質が向上している。特に、BITやセグメント木などの高度なデータ構造を実装できる能力は、競技プログラミングにおいて非常に価値がある。

新しいファイル（ABC431以降）では命名規則やコード構造が改善されており、全体的なコード品質は向上傾向にある。しかし、古いファイルのリファクタリング、型ヒントの一貫した使用、例外処理の改善など、さらなる改善の余地がある。

ヒューリスティックコンテストでのC++実装も高品質であり、幅広い技術スタックに対応できる能力を示している。

---

## 8. 次のステップ

### 8.1 即座に実施すべきこと

1. **ファイル整理**
   - `tempCodeRunnerFile.py` を削除
   - 未完成ファイルを整理（完成版と未完成版を明確に区別）
   - ファイル名を統一（問題名や番号で統一）

2. **バグ修正**
   - 例外処理の改善（`except:`の使用を避ける）
   - 未初期化変数を修正
   - 不完全な実装を完成させる

### 8.2 短期（1週間〜1ヶ月）

1. **命名規則の統一**
   - 古いファイルの大文字変数を小文字+アンダースコアに変更
   - 組み込み関数（`list`, `len`）を変数名として使用しない
   - 意味のある変数名を使用

2. **コメントの追加**
   - すべての関数にdocstringを追加
   - 複雑なロジックにコメントを追加
   - デバッグ用コメントを削除

3. **テンプレートファイルの改善**
   - 型ヒントの追加
   - より詳細なdocstringの追加

### 8.3 中期（1ヶ月〜3ヶ月）

1. **関数化とリファクタリング**
   - 重複コードを関数に抽出
   - 再利用可能な関数を作成
   - コードの構造化

2. **型ヒントの使用**
   - すべての関数に型ヒントを追加
   - テンプレートファイルにも型ヒントを追加
   - 型安全性を向上

3. **コードレビューの習慣化**
   - 提出前にコードを見直す
   - バグの早期発見
   - エッジケースの考慮

### 8.4 長期（3ヶ月以上）

1. **最適化の習慣化**
   - 最初から効率性を考慮する
   - メモリ効率と時間効率の両方を考慮
   - アルゴリズムの選択を最適化

2. **テストの習慣化**
   - エッジケースの考慮
   - テストケースの作成
   - デバッグの効率化

3. **コード品質の向上**
   - ベストプラクティスの適用
   - 設計パターンの学習
   - コードの可読性と保守性の向上

---

## 9. 詳細な統計

### 9.1 ファイル別の特徴

**高品質なファイル（参考にすべき例）:**
- `ABC430/c.py`: 高度なデータ構造、適切な命名、main関数の使用
- `ABC409/C.py`: 詳細なコメント、適切な命名、アルゴリズムの説明
- `ABC435/d.py`: 適切なクラス設計、コメント、型ヒントの一部使用
- `MoneyForward/5.py`: 型ヒント付き、適切な命名、関数化
- `Template/BFS.py`, `Template/DFS.py`, `Template/SegTree.py`: 高品質なテンプレート、詳細なdocstring
- `A063_AI.py`: 最適化版、コメントが充実

**改善が必要なファイル:**
- `ABC435/b.py`: 非効率な実装（O(N^3)）、変数名が不明確
- `ABC431/c.py`: 例外処理が不適切（`except:`の使用）
- `ABC431/b.py`: 大文字変数の使用、変数名が不明確
- 古いファイル（ABC407-ABC430の一部）: 命名規則の改善が必要

### 9.2 アルゴリズム別の実装状況

| アルゴリズム | 実装ファイル数 | 品質評価 | 備考 |
|------------|------------|---------|------|
| DFS | 複数 | ⭐⭐⭐⭐ | テンプレートファイルあり |
| BFS | 1 | ⭐⭐⭐⭐⭐ | テンプレートファイルあり |
| 動的プログラミング | 複数 | ⭐⭐⭐ | - |
| BIT・セグメント木 | 2 | ⭐⭐⭐⭐⭐ | テンプレートファイルあり |
| 差分配列 | 2 | ⭐⭐⭐⭐ | - |
| 尺取り法 | 1 | ⭐⭐⭐⭐ | - |
| 累積和 | 複数 | ⭐⭐⭐ | - |
| 二分探索 | 1 | ⭐⭐⭐⭐ | `bisect`モジュールを使用 |
| グラフ探索 | 複数 | ⭐⭐⭐⭐ | Nodeクラスを使用 |

### 9.3 命名規則の使用状況

- **大文字変数**: 約20%のファイルで使用（減少傾向）
- **小文字変数**: 約80%のファイルで使用（増加傾向）
- **適切な命名**: 約30%のファイル（ABC430/c.py, ABC409/C.py, ABC435/d.py, MoneyForward/5.pyなど）
- **組み込み関数の上書き**: 約5%のファイル（減少傾向）
- **型ヒントの使用**: 約10%のファイル（増加傾向）

### 9.4 テンプレートファイルの状況

- **BFS.py**: 幅優先探索のテンプレート、詳細なdocstring
- **DFS.py**: 深さ優先探索のテンプレート、詳細なdocstring
- **SegTree.py**: セグメント木のテンプレート、詳細なdocstring
- **Node.py**: グラフ構造のノードクラス

**評価:**
- テンプレートファイルは高品質
- 型ヒントの追加が推奨される
- より多くのアルゴリズムのテンプレート化が推奨される

### 9.5 ヒューリスティックコンテスト

- **C++ファイル数**: 26個
- **実装されているアルゴリズム**: ビームサーチ、ILS、ダイクストラ法など
- **品質評価**: ⭐⭐⭐⭐

---

## 10. 時系列での改善傾向

### 改善が見られる点

1. **命名規則**: ABC431以降のファイルでは命名規則が改善
2. **コード構造**: 新しいファイルではコメントが増加
3. **テンプレートファイル**: 再利用可能なコードの作成
4. **型ヒント**: 就職活動のコーディングテストで使用
5. **ファイル管理**: 一時ファイルが減少（1個に）

### 改善が必要な点

1. **古いファイルのリファクタリング**: ABC407-ABC430の一部
2. **例外処理**: `except:`の使用を避ける
3. **型ヒントの一貫性**: すべてのファイルで使用
4. **効率性**: 一部のファイルで非効率な実装が存在

---

*分析日: 2025年1月*
*分析対象: 115個のPythonファイル、26個のC++ファイル*
*分析範囲: AtCoder、paiza、就職活動、大学課題、ヒューリスティックコンテスト*
