情報数学 2A レポート
レポートの概要
⚫ Word(または LaTeX)を使用して A4 文書数枚のレポートを作成し、プリンターで片面印刷したも
のをホッチキスで綴じて提出しなさい。一部の修正であれば手書きでも構わない。
⚫ 表紙を作らず、1 枚目の先頭にタイトル(「情報数学 2A レポート」)と学籍番号、名前を書く。
⚫ 問題には、基本・実用・応用の 3 レベルが段階的に用意されている。かならず、基本レベルから始
めて、実用レベル、応用レベルへと順に進んでいくこと。
基本レベル…解説付きのやさしい問題。基本レベルまでだと単位を落とす可能性がある。
実用レベル…授業中の演習問題程度。普通はここまで答えることが求められる。
応用レベル A,B…授業中の応用問題程度。答えればさらに点数を加算(一部でも可)。なお、A
と B はどちらか一方だけやることも、両方やることもできるように作られている。
⚫ 変数名・配列名・関数名は問題文で指定されたものを使用すること。
⚫ レポートの名前より下には、以下の内容を順番に書くこと。
※ レポートは、あなたが最終的に到達したレベルのプログラムに基づいて書く。たとえば、実用レベルまで進
められたなら、基本レベルのプログラムリストや実行結果を書く必要はない。
 (先頭に)基本・実用・応用のどのレベルまでやったのか。
 端末アプリからコピーして貼り付けたプログラムリスト。なお、コピーする際、Emacs を起動
していない状態で「./prog30」を実行し、課題のソースファイル名を入力すると、行番号付
きのプログラムリストがウィンドウ全体に表示される。これを抜けがないように範囲選択
し、Ctrl-Shift-C キーでコピーするといい。Word に貼り付けた後は、プログラムリスト
全体を範囲指定してから以下の設定を行なうこと。
⚫ フォントを Consolas の 10.5 ポイントに変更→Windows 付属の固定幅フォントを設定。
⚫ 右クリックして「段落」を選択し、「1 ページの行数を指定時に文字を行グリッド線に合わ
せる」のチェックを外す→行間が(読める範囲で)詰まって紙を節約できる。
※ Windows 用 Word のオプション設定で、「詳細設定」の一番下の画面にある「バックスラッシュを円記号(\)
に変換する」のチェックを外すと、プログラムリストの「\」が円記号になるのを防ぐことができる。さらに、
「半角文字と全角文字の文字幅を調整する」のチェックを外すと、リストの文字の位置が揃う。設定は任意。
 プログラムの解説。これは、プログラムリストと同じくらい重要。たとえば、「x 行～y 行では、
(中略)するために、(中略)の処理をしている。」といった具合に、まとめて説明できる 1 行
から数行の部分に分けて、その部分の目的と方法をわかりやすい文章で解説すること。
× 「整数型の変数 mensu を宣言し、初期値 6 を代入している。」(それは見ればわかる)
○ 「任意の面数のダイスを再現するため、整数型の変数 mensu を宣言する。初期値には、一
般的な 6 面ダイスを再現するために 6 を代入している。」
 端末アプリからコピーして貼り付けたプログラムの実行結果。作成したプログラムの機能
を充分に示すだけの実行結果が必要だ。いつものように、Emacs 実行中に下 1/3 の領域で
実行するか、(応用問題まで進んだら)Emacs を起動していない状態で「./実行ファイル名」
を実行した結果をコピーして使う。コピー範囲には、「./実行ファイル名」の行を含めるこ
と。Word に貼り付けた後は、プログラムリストと同様の設定を行なう。
他人や生成系 AI のプログラムや解説をコピーすると大幅減点される。課題は自分で作ろう。
1

情報数学 2A 2025 年 12 月 22 日
問題 回文の表示と判定を行なう
日本語の文字列を 1 行キー入力すると、その文字を逆順に並び替えた文字列を生成し、
回文かどうかを判定するプログラムを作成しなさい(下図参照)
基本 たけやぶやけた たけやぶやけた 回文です (入力)
(逆順表示)
(判定表示)
あいうえお おえういあ 回文ではありません (入力)
(逆順表示)
(判定表示)
(1) 授業で使用しない番号(40 番を推奨)でソースファイル(progNN.c)を新規作成する。
(2) ヘッダーファイル stdio.h, string.h, stdbool.h を「#include <ファイル名>」で読み込む。
(3) 関数 main の内部で文字型の配列 motoと gyakuを宣言する。要素数はどちらも 100 とする。
(4) 授業中に作成した関数 safegets を今回のソースファイルにコピーして、末尾で改行する入力案
内(文章は自分で考えること)の表示とキー入力に利用し、配列 moto に日本語の文字列(末尾
の改行文字を削除したもの)を格納する。
(5) 文字を逆順に並び替えた文字列を配列 gyaku に格納するため、for 文による繰り返し処理を
行なう。繰り返しを制御する整数型の変数(カウンター)は i にすること。for 文に記述する
(初期処理; 継続条件; 更新処理) と、直後の波カッコ {} 内に記述する繰り返す処理につ
いては、以下の記述を参考にして自分で考えること。
※ この処理の目標は、配列 moto に格納された文字列の末尾から先頭に向かって、日本語 1 文字分にあたる
3 つの値を取り出し、配列 gyakuの先頭から順番にその値を格納することだ。
ヒント 日本語 1 文字を示す値(文字コード)は、C 言語の文字型(char)が扱える値の範囲より大きい
ため、配列の 3 つの要素に値が分かれて格納される。つまり、日本語 1 文字を処理するに
は、それに対応する 3 つの要素をまとめて扱う必要がある。
① 配列 motoには日本語の文字列が格納済みだ。先頭の文字の値は moto[0], moto[1],
moto[2]に、次の文字の値は moto[3], moto[4], moto[5]に、とそれぞれ 3 つの要素に
分割された値が格納されている。
※ 文字列が「わせ」なら、moto[0]～moto[2]に「わ」の値、moto[3]～moto[5]に「せ」の値が格納済み。
② 終端印のヌル文字(\0)は、日本語ではないので 1 つの要素に格納される。この要素の添え
字は、文字列の長さ(要素数)を調べる関数 strlen を使って strlen(moto) と書ける。
※ 文字列が「わせ」なら strlen(moto)が 6を返すので、moto[6] にヌル文字が格納されている。
③ 末尾の文字の値は、ヌル文字の要素の直前の 3 つの要素に分かれて格納され、それらの添
え字は、strlen(moto) – 3, strlen(moto) – 2, strlen(moto) – 1 と書ける。
※ 文字列が「わせ」なら、文字「せ」の値を格納した要素の添え字は 6 – 3, 6 - 2, 6 – 1 で計算。
 for 文の繰り返す処理を書く波カッコ {} の内部では、配列 motoから配列 gyakuに 3 つ
の要素の値をコピーする。具体的には、gyaku[j]に moto[i] の値、gyaku[j + 1]に
moto[i + 1]の値、gyaku[j + 2]に moto[i + 2]の値を設定する。
 1 回目の繰り返しでは、moto[i]～moto[i + 2]で末尾の文字の値を取り出したい。そのた
めに、for 文の初期処理で、カウンター用の変数 iを宣言し、ヒントを参考にした適切な式で
に、for 文の更新処理で、現在の iの値を適切な数だけ減らした値を iに設定(更新)せよ。
初期値を設定せよ。
 2 回目以降は、moto[i]～moto[i + 2]で 1 つずつ手前の文字を取り出したい。そのため
2
 for 文の継続条件には、減らしていく変数 iの値が配列の添え字として使える範囲にあるこ
と、つまり「変数 iの値が 0以上である」ことを条件式として書けばいい。

 配列 gyaku の添え字にする変数 j の宣言は、for 文の初期処理の変数 i の処理の後に「,」
で区切って書いて初期値 0 を設定する。さらに、for 文の更新処理の変数 i の処理の後に
も、「,」で区切って現在の jの値を適切な数だけ増やした値を jに設定する式を書こう。
(6) for 文の直後で gyaku[strlen(moto)]にヌル文字'\0'を設定し、配列 gyaku を文字列形式
にする。これを忘れると、配列 gyakuの残りの要素(内容は不定)が画面に表示されてしまう。
(7) 逆順に並び替えた文字列を puts で画面に表示する。画面上では、(5)で入力した文字列の真
下に表示されるはずだ。
(8) 入力した日本語の文字列が回文かどうかを判定し、回文なら「回文です」、そうでなければ「回
文ではありません」と画面に表示した後、最後に 2 回改行する。判定には、2 つの文字列を比
較する関数 strcmp を使うこと→一致するなら strcmp の戻り値が 0になる。


実用 「キー入力した日本語の文字列」と「その文字を逆順に並び替えた文字列」、「回文かどう
かの判定結果」をまとめた構造体を設計して利用しなさい。さらに、逆順に並び替える処理と回
文かどうかの判定処理を、ユーザー定義関数 make_kaibun に分離するようプログラムを書き換
えなさい。

(1) 関数 main より前に、別名 Kaibun を付けた構造体の型を、typedef と struct を使って宣言しな
さい(構造体のタグ名は省略すること)。この構造体のメンバーには、文字型の配列 motoと
gyaku (いずれも要素数 100)、論理型(bool)の変数 dekitaを記述せよ。
(2) 関数 main の内部で、配列 motoと配列 gyakuの宣言を削除する。代わりに、別名 Kaibun を
付けた構造体の変数 my_kaibunの宣言を追加せよ(初期化は不要)。
(3) 関数 main より前に、ユーザー定義関数 make_kaibun のプロトタイプ宣言を追加する(末尾に
「;」が必要)。関数の戻り値は void (戻り値なし)とし、仮引数(丸カッコ内)には「Kaibun
*ptr」を指定する→別名 Kaibun を付けた構造体の変数の位置を示すポインター変数。
(4) 関数 main より後に、ユーザー定義関数 make_kaibun の定義(内容)を記述する。最初の行は、
(3)で書いたプロトタイプ宣言をコピーし、末尾の「;」を削除すればいい。関数の内部には、関
数 mainから for 文と直後のヌル文字設定部分を切り取って貼り付ける(main からは削除)。
(5) 関数 make_kaibun の内容のうち、配列 motoと gyakuを利用している式を書き換える。構造
体の変数 my_kaibunのメンバーmotoと gyakuを、ポインター変数 ptrで間接参照するよう
にしなさい。ヒント: アロー表現「ポインター変数名->メンバー名」を使う。
(6) 関数 main で関数 safegets を呼び出した直後に、関数 make_kaibun を呼び出す処理を追加し
なさい。このときの実引数には、構造体の変数 my_kaibun のポインター値を指定する。
(7) 関数 main 関数の内部に残された motoや gyakuを、構造体の変数 my_kaibunのメンバー
moto や gyaku を利用する式に書き換えなさい。ヒント: 「変数名.メンバー名」を使う。
(8) 関数 make_kaibun の末尾に、関数 mainから回文判定の if文をコピーしなさい(main にも残
す)。関数 make_kaibun に貼り付けた if 文の条件式をアロー表現で書き替えた後、条件成立時
の処理を「ptr->dekitaに trueを設定」、不成立時の処理を「ptr->dekitaに falseを設
定」する式に書き換えよ→回文の判定はここで行ない、結果の表示は関数 main で行なう。さ
らに、「元の文字列の長さが 0 より大きい」を意味する 2 つめの条件式を追加し、元の条件式
と「&&」(かつ)で結合しなさい。なお、この回文判定の if文全体と同じ処理を、ひとつの代入
式で書ければ加点する。
(9) 関数 mainの回文判定の if文は、条件式全体を my_kaibun.dekitaに置き換えなさい。こ
れにより、dekitaが trueの場合は条件が成立し、falseの場合は条件が成立しなくなる。


応用 A 「./プログラム名 たけやぶやけた」のように、日本語の文字列をコマンドライン引数で
指定するようにプログラムを変更しなさい。さらに、コマンドライン引数が複数指定されたとき
には、それらを順番にすべて処理できるようにしなさい。
(1) 関数 main の先頭行の仮引数(丸カッコ内)を argcと argvに変更→前回のプリント参照。
(2) 関数 main でのキー入力は不要になるので、それに対応する関数 safegets の呼び出しを削除
せよ。その後、関数 safegets のプロトタイプ宣言と関数本体も削除していい。
(3) 関数 main の変数宣言の直後に、コマンドライン引数がひとつも指定されていないことを条件
式とする if 文を追加し、{} 内に「コマンドライン引数に日本語の文字列を指定してください」
と表示して改行し、終了コード 1 でプログラムを終了する処理を書きなさい。
(4) (3)の処理より後に、my_kaibun.moto に最初のコマンドライン引数の文字列(先頭文字の位
置は argv[1])を strcpy でコピーし、my_kaibun.moto の内容を puts で画面に表示せよ。こ
の時点で実行し、最初のコマンドライン引数だけを利用して動作することを確認しよう。
(5) コマンドライン引数を 2 個以上指定する場合に対応させなさい。カウンター変数を i とする
for 文を追加し、最初のコマンドライン引数の添え字 1 から、最後のコマンドライン引数の添え
字 argc - 1 まで、i の値を 1 ずつ増やしながら繰り返せばいい。for 文の {} 内には、(4)で
作成したコマンドライン引数(位置は argv[i]に変更)のコピー、元の文字列の表示、関数
make_kaibun の呼び出し、逆順文字列の表示、回文判定結果の表示をすべて含めること。

応用 B 「./プログラム名 –f kaibun.txt」のように、-fとファイル名 1 個だけをコマンドライ
ン引数で指定した場合、そのファイルの内容を 1 行ずつ読んで、それぞれ回文かどうか調べる
プログラムに書き換えなさい。読ませるファイルも各自作成すること。
(1) (応用 A をやっていなければ)応用 A の(1)(2)と同じ作業を行なう。
を指定してください』と表示して改行し、終了コード 1 でプログラムを終了する処理を書け。
応用 A をやっていないなら、表示するメッセージから 日本語の文字列か を削除する。
(2) 関数 main の変数宣言の直後に、コマンドライン引数がひとつも指定されていないことを条件
式とする if 文を追加し、{} 内に『コマンドライン引数に日本語の文字列か「-f ファイル名」
を指定してください』と表示して改行し、終了コード 1 でプログラムを終了する処理を書け。
応用 A をやっていないなら、表示するメッセージから 日本語の文字列か を削除する。
(3) (2)の処理の後に新たな if 文を追加し、「コマンドライン引数が 2 つ存在する」という条件と、
「最初のコマンドライン引数(位置は argv[1])が "-f" と一致する」という条件を、「&&」で結合した条件式を書きなさい。if 文の {} 内には、以下の処理を順番に書くこと。
① ファイルポインター用のポインター変数を宣言し、2 つめのコマンドライン引数(位置は
argv[2])をファイル名と見なし、読み込みモードでオープンする。もし、オープンに失敗し
た場合は、「ファイル名を開けませんでした」と実際のファイル名を埋め込んだメッセージを
表示して改行し、終了コード 2 でプログラムを終了しなさい。
② while 文の条件式で関数 fgets を使って、オープンしたファイルの内容を先頭から末尾まで 1
行ずつ読み込み、my_kaibun.motoに文字列として格納するようにしなさい。
③ while 文の {} 内に、my_kaibun.motoに格納した文字列の末尾の文字が改行'\n'なら、
ヌル文字'\0'で上書きする処理を書け。
④ while 文の {} 内に、③の処理の後に、元の文字列の表示、make_kaibun関数の呼び出し、
逆順文字列の表示、回文判定結果の表示を含める。
⑤ while 文を終了した後でファイルをクローズする。
(4) 応用 A を済ませている場合は、(3)の条件が成立しなかったときにだけ、応用 A の(5)で作成した for 文が実行されるようにしなさい。これで、応用 Aと応用 B の両立が可能になる。
